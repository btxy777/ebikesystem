<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ebike.mapper.UserMapper">
    <resultMap id="userMap" type="com.ebike.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="phone" property="phone" />
        <result column="role" property="role" />
        <result column="create_time" property="createTime" />
        <result column="status" property="status" />
    </resultMap>

    <select id="selectByUsername" parameterType="string" resultMap="userMap">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="selectByPhone" parameterType="string" resultMap="userMap">
        SELECT * FROM users WHERE phone = #{phone}
    </select>

    <select id="selectById" parameterType="long" resultMap="userMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="userMap">
        SELECT * FROM users ORDER BY create_time DESC
    </select>

    <select id="selectByRole" parameterType="int" resultMap="userMap">
        SELECT * FROM users WHERE role = #{role} ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.ebike.entity.User">
        INSERT INTO users (username, password, phone, role, create_time, status)
        VALUES (#{username}, #{password}, #{phone}, #{role}, #{createTime}, #{status})
    </insert>

    <update id="update" parameterType="com.ebike.entity.User">
        UPDATE users SET
        username = #{username},
        <if test="password != null and password != ''">
        password = #{password},
        </if>
        phone = #{phone},
        role = #{role},
        status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM users
    </select>
</mapper>
